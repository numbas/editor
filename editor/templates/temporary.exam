{% autoescape off %}
{
	name: {{ exam.name }}
	{{ exam.content }}
	questions: [
	{% for question in exam.questions.all %}
		{
			name: {{ question.name }}
			{{ question.content }}
		}
	{% endfor %}
	]
}
{% endautoescape %}