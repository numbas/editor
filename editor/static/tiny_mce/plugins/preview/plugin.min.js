/**
 * plugin.js
 *
 * Released under LGPL License.
 * Copyright (c) 1999-2015 Ephox Corp. All rights reserved
 *
 * License: http://www.tinymce.com/license
 * Contributing: http://www.tinymce.com/contributing
 */

/*global tinymce:true */

tinymce.PluginManager.add('preview', function(editor) {
	var settings = editor.settings, sandbox = false && !tinymce.Env.ie;

    function make_preview() {
        var container = document.querySelector('#previewModal .preview-content');
        container.innerHTML = editor.getContent({keepGapfills:true});
        Numbas.jme.variables.DOMcontentsubvars(container,viewModel.questionScope());
        $(container).data('jme-scope',viewModel.questionScope());
        MathJax.Hub.Queue(["Typeset",MathJax.Hub,container]);
    }

	editor.addCommand('mcePreview', function() {
        $('#previewModal')
            .modal('show')
            .on('hide.bs.modal',function() {
                $('#previewModal .regenerate-button').off('click');
            })
        ;
        $('#previewModal .regenerate-button')
            .on('click',function() {
                viewModel.generateVariablePreview();
                make_preview();
            })
        ;
        make_preview();

        return;
	});

	editor.addButton('preview', {
		title: 'Preview',
		cmd: 'mcePreview'
	});

	editor.addMenuItem('preview', {
		text: 'Preview',
		cmd: 'mcePreview',
		context: 'view'
	});
});

